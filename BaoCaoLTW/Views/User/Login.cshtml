
@{
    ViewBag.Title = "Đăng nhập";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2 style="text-align:center; margin-top:60px">Đăng nhập</h2>
<h5 style="text-align:center;color:red"> @ViewBag.Thongbao </h5>

<table align="center">
    @using (Html.BeginForm())
    {
        <tr style="height : 40px">
            <td>Tên đăng nhập :</td>
            <td>
                @Html.TextBox("tenDN")
                @ViewData["Loi1"]
            </td>
        </tr>
        <tr style="height : 40px">
            <td>Mật khẩu :</td>
            <td>
                @Html.Password("matkhau")
                @ViewData["Loi2"]
            </td>
        </tr>
            <tr style="height : 40px">
                <td></td>
                <td>
                    <input type="submit" value="Đăng nhập" />
                </td>
            </tr>
    }

</table>
